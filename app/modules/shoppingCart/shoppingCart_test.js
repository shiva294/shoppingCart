'use strict';

describe('myApp.shoppingCart module', function() {

  beforeEach(module('myApp.shoppingCart'));

  describe('shoppingCart controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var shoppingCartCtrl = $controller('shoppingCartCtrl');
      expect(shoppingCartCtrl).toBeDefined();
    }));

  });
});
